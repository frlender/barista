<div id="{{ div_string }}" class="{{ span_class }}">
    <div class="cmap-card">
        <a href="{{url}}">
            <div class='cmap-card-title'>{{ title }}</div>
            <p class="cmap-card-subtitle">{{ subtitle }}</p>
        </a>
    </div>
</div>
