<hr>
<div class="row hidden-xs">
	<p class="col-sm-offset-1 col-sm-5 pull-left">&copy {{ year }} {{ organization }} all rights reserved</p>
	<div class="col-sm-5">
	{{#each logo_objects}}
		<a href="{{ url }}"><img class="pull-right cmap-footer-image" src="{{ logo }}"></a>
	{{/each}}
	</div>
	<div class="cmap-spacer-medium"></div>
</div>

<div class="row visible-xs">
	<p class="col-xs-12 cmap-center-text">&copy {{ year }} {{ organization }} all rights reserved</p>
	<div class="col-xs-12 cmap-center-text">
	{{#each logo_objects}}
		<a href="{{ url }}"><img class="cmap-footer-image" src="{{ logo }}"></a>
	{{/each}}
	</div>
	<div class="cmap-spacer-medium"></div>
</div>
